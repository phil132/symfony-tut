{% if date(message.created_at) < date('-1year') %}
    <div style="color: red">This message is old</div>
{% else %}
    <div style="color: grey">{{ message.created_at|date('d/m/Y') }}</div>
{% endif %}